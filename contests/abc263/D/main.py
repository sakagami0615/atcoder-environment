#!/usr/bin/env python3
import sys


def solve(N: int, L: int, R: int, A: "List[int]"):

    """
    min_[0<=x<=y<=N]{ Lx + (A_cumsum[y] - A_cumsum[x]) + R*(N - y) }
    min_[0<=x<=y<=N]{ Lx + (A_cumsum[y] - A_cumsum[x]) + (RN - Ry) }
    RN + min_[0<=x<=y<=N]{ Lx - A_cumsum[x] + A_cumsum[y] - Ry }
    RN + min_[0<=x<=N]{ Lx - A_cumsum[x] + min_[x<=y<=N]{ A_cumsum[y] - Ry } }
    """
    A_cumsum = [0] * (N + 1)
    for i in range(N):
        A_cumsum[i + 1] = A_cumsum[i] + A[i]

    cumsum_min = [0] * (N + 1)
    min_val = float('Inf')
    for y in range(N, -1, -1):
        min_val = min(min_val, A_cumsum[y] - R*y)
        cumsum_min[y] = min_val

    ans = float('Inf')
    for x in range(N + 1):
        val = L * x - A_cumsum[x] + cumsum_min[x]
        ans = min(ans, val)

    ans += R*N

    #ans = float('Inf')
    #for x in range(N + 1):
    #    for y in range(x, N + 1):
    #        ans = min(ans, L*x + R*(N-y) + (A_cumsum[y] - A_cumsum[x]))

    print(ans)



# Generated by 2.12.0 https://github.com/kyuridenamida/atcoder-tools  (tips: You use the default template now. You can remove this line by using your custom template)
def main():
    def iterate_tokens():
        for line in sys.stdin:
            for word in line.split():
                yield word
    tokens = iterate_tokens()
    N = int(next(tokens))  # type: int
    L = int(next(tokens))  # type: int
    R = int(next(tokens))  # type: int
    A = [int(next(tokens)) for _ in range(N)]  # type: "List[int]"
    solve(N, L, R, A)

if __name__ == '__main__':
    main()
