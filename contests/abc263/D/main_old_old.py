#!/usr/bin/env python3
import sys


def solve(N: int, L: int, R: int, A: "List[int]"):

    INF = float('Inf')
    A_rev = list(reversed(A))

    # 0: xchange, 1: not change
    r_dp = [[INF] * (N + 1) for _ in range(2)]
    l_dp = [[INF] * (N + 1) for _ in range(2)]
    r_dp[0][0] = 0
    l_dp[0][0] = 0

    # left process
    for i in range(1, N + 1):
        l_dp[0][i] = l_dp[0][i - 1] + L
        l_dp[1][i] = l_dp[0][i - 1] + A[i - 1]

    l_idx = N - 1
    for i, (a, b) in enumerate(zip(l_dp[0], l_dp[1])):
        if a > b:
            l_idx = i - 2
            break

    # right process
    for i in range(1, N + 1):

        r_dp[0][i] = r_dp[0][i - 1] + R
        r_dp[1][i] = r_dp[0][i - 1] + A_rev[i - 1]

    r_idx = N - 1
    for i, (a, b) in enumerate(zip(r_dp[0], r_dp[1])):
        if a > b:
            r_idx = i - 2
            break
    r_idx = N - 1 - r_idx

    ans = (l_idx + 1) * L + (N - r_idx) * R

    #for i in range(l_idx + 1, r_idx):
    #   ans += A[i]

    print()
    print(l_dp[0])
    print(l_dp[1])
    print()
    print(r_dp[0])
    print(r_dp[1])
    print()
    print(l_idx, r_idx)

    print(ans)




# Generated by 2.12.0 https://github.com/kyuridenamida/atcoder-tools  (tips: You use the default template now. You can remove this line by using your custom template)
def main():
    def iterate_tokens():
        for line in sys.stdin:
            for word in line.split():
                yield word
    tokens = iterate_tokens()
    N = int(next(tokens))  # type: int
    L = int(next(tokens))  # type: int
    R = int(next(tokens))  # type: int
    A = [int(next(tokens)) for _ in range(N)]  # type: "List[int]"
    solve(N, L, R, A)

if __name__ == '__main__':
    main()
