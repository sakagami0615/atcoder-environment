#!/usr/bin/env python3
import sys
from atcoder import dsu
from bisect import bisect_left
from collections import defaultdict
from collections import deque
# import pypyjit
# pypyjit.set_param(inlining=0)     # or pypyjit.set_param('max_unroll_recursion=-1')

sys.setrecursionlimit(10 ** 6)

INF = 2 ** 64 - 1


def solve(L: int, R: int, D: int, U: int):
    
    def norm():
        nonlocal L, R, D, U
        if L < 0 and R < 0:
            L, R = -L, -R        
        if D < 0 and U < 0:
            D, U = -D, -U

    def split():
        rect_list, axis_list = [], []
        is_o = False

        if (R >= 0 and L <= 0) and (U >= 0 and D <= 0):
            axis_list.append(())
            
            is_o = True

        return rect_list, axis_list, is_o

    def calc_axis(v):
        return v // 2

    def calc_rect(x, y):
        #min_xy = min(x, y)
        return 0


    norm()
    rect_list, axis_list, is_o = split()

    ans = 0
    for l, r, d, u in rect_list:
        ans += calc_rect(r, u)
        ans -= calc_rect(l, u)
        ans -= calc_rect(r, d)
        ans += calc_rect(l, d)
    
    for v_min, v_max in axis_list:
        ans += calc_axis(v_max)
        ans -= calc_axis(v_min)
    
    if is_o:
        ans += 1
    
    print(ans)


# Generated by 2.16.0 https://github.com/kyuridenamida/atcoder-tools  (tips: You use the default template now. You can remove this line by using your custom template)
def main():
    def iterate_tokens():
        for line in sys.stdin:
            for word in line.split():
                yield word
    tokens = iterate_tokens()
    L = int(next(tokens))  # type: int
    R = int(next(tokens))  # type: int
    D = int(next(tokens))  # type: int
    U = int(next(tokens))  # type: int
    solve(L, R, D, U)

if __name__ == '__main__':
    main()
