#!/usr/bin/env python3
import sys


"""
def solve(N: int, K: int, A: "List[int]"):

    a_list = [0] * (N + 1)
    for a in A:
        a_list[a] = a

    for i in range(1, N + 1):
        if a_list[i] == 0:
            a_list[i] = a_list[i - 1]

    pick_total_num = [0, 0]
    curr_turn = 0


    deck_num = N
    while deck_num > 0:
        pick_num = a_list[deck_num]
        pick_total_num[curr_turn] += pick_num
        deck_num -= pick_num
        curr_turn = abs(curr_turn - 1)

    print(pick_total_num[0])
"""

def solve(N: int, K: int, A: "List[int]"):

    dp = [0] * (N + 1)

    for i in range(N + 1):
        for a in A:
            if i < a:
                break
            dp[i] = max(dp[i], i - dp[i - a])

    print(dp[-1])


# Generated by 2.12.0 https://github.com/kyuridenamida/atcoder-tools  (tips: You use the default template now. You can remove this line by using your custom template)
def main():
    def iterate_tokens():
        for line in sys.stdin:
            for word in line.split():
                yield word
    tokens = iterate_tokens()
    N = int(next(tokens))  # type: int
    K = int(next(tokens))  # type: int
    A = [int(next(tokens)) for _ in range(K)]  # type: "List[int]"
    solve(N, K, A)

if __name__ == '__main__':
    main()
