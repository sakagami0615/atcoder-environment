#!/usr/bin/env python3
import sys
sys.setrecursionlimit(10 ** 8)

def solve(N: int, M: int, A: "List[int]", B: "List[int]"):

    graph = [[] for _ in range(N)]
    for a, b in zip(A, B):
        a, b = a - 1, b - 1
        graph[a].append(b)
        graph[b].append(a)

    INF = float('INF')
    is_visit = [INF] * N
    edge_set = set()

    search_cnt = -1
    close_cnt = 0
    def dfs(i, prev_i):
        nonlocal is_visit
        nonlocal edge_set
        nonlocal close_cnt

        if prev_i != None:
            curr_edge = (min((i, prev_i)), max((i, prev_i)))
            if is_visit[i] == search_cnt:
                if curr_edge not in edge_set:
                    close_cnt += 1
                    #print(('★', i, prev_i))
                edge_set.add(curr_edge)
                return

            edge_set.add(curr_edge)

        #print((i, prev_i))

        is_visit[i] = search_cnt
        for n in graph[i]:
            #if is_flag[n] and is_visit[n] < search_cnt:
            #    continue
            if (prev_i is None) or (n != prev_i):
                dfs(n, i)

    for i, t in enumerate(is_visit):
        search_cnt += 1
        if t == INF:
            dfs(i, None)
    
    print(close_cnt)





# Generated by 2.12.0 https://github.com/kyuridenamida/atcoder-tools  (tips: You use the default template now. You can remove this line by using your custom template)
def main():
    def iterate_tokens():
        for line in sys.stdin:
            for word in line.split():
                yield word
    tokens = iterate_tokens()
    N = int(next(tokens))  # type: int
    M = int(next(tokens))  # type: int
    A = [int()] * (M)  # type: "List[int]"
    B = [int()] * (M)  # type: "List[int]"
    for i in range(M):
        A[i] = int(next(tokens))
        B[i] = int(next(tokens))
    solve(N, M, A, B)

if __name__ == '__main__':
    main()
