#!/usr/bin/env python3
import sys
from atcoder import dsu
from bisect import bisect_left
from collections import defaultdict
from collections import deque
# import pypyjit
# pypyjit.set_param(inlining=0)     # or pypyjit.set_param('max_unroll_recursion=-1')

sys.setrecursionlimit(10 ** 6)

INF = 2 ** 64 - 1

MOD = 10007  # type: int

"""
{ [N / M ] } mod P
= { (N % P - N % M) * M^(-1) } mod P
"""

def mod_pow(x: int, y: int, mod: int) -> int:
    num = 1
    while y > 0:
        if y & 1:
            num = (num * x) % mod
        x = (x ** 2) % mod
        y >>= 1
    return num


def mod_div(a: int, b: int, m: int) -> int:
    return a * pow(b, m - 2, m) % m


def solve(K: int, M: int, C: list[int], L: list[int]):
    
    def calc(c, l, mod):
        res = 0
        unit = 10
        x = 1
        while l > 0:
            if l % 2 == 1:
                res = (res * unit + x) % mod
            x = (x * unit + x) % mod
            unit = (unit ** 2) % mod
            l //= 2
        return (c * res) % mod

    def N(mod):
        n = 0
        for c, l in zip(C, L):
            n = n * mod_pow(10, l, mod) + calc(c, l, mod)
            n %= mod 
        return n
    
    ans = (N(MOD) - N(M)) * mod_div(1, M, MOD)
    ans %= MOD
    print(ans)


# Generated by 2.16.0 https://github.com/kyuridenamida/atcoder-tools  (tips: You use the default template now. You can remove this line by using your custom template)
def main():
    def iterate_tokens():
        for line in sys.stdin:
            for word in line.split():
                yield word
    tokens = iterate_tokens()
    K = int(next(tokens))  # type: int
    M = int(next(tokens))  # type: int
    c = [int()] * (K)  # type: list[int]
    l = [int()] * (K)  # type: list[int]
    for i in range(K):
        c[i] = int(next(tokens))
        l[i] = int(next(tokens))
    solve(K, M, c, l)

if __name__ == '__main__':
    main()
