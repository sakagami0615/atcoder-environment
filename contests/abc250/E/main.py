#!/usr/bin/env python3
import sys
from copy import deepcopy

YES = "Yes"  # type: str
NO = "No"  # type: str


# TLE
def solve(N: int, a: "List[int]", b: "List[int]", Q: int, x: "List[int]", y: "List[int]"):

    a_set_list = [None]*(N + 1)
    b_set_list = [None]*(N + 1)

    a_set_list[0] = set()
    b_set_list[0] = set()

    for i in range(1, N + 1):
        a_set_list[i] = deepcopy(a_set_list[i - 1])
        b_set_list[i] = deepcopy(b_set_list[i - 1])
        a_set_list[i].add(a[i - 1])
        b_set_list[i].add(b[i - 1])

    for i in range(Q):
        x_q = x[i]
        y_q = y[i]

        if a_set_list[x_q] == b_set_list[y_q]:
            print(YES)
        else:
            print(NO)


# Generated by 2.12.0 https://github.com/kyuridenamida/atcoder-tools  (tips: You use the default template now. You can remove this line by using your custom template)
def main():
    def iterate_tokens():
        for line in sys.stdin:
            for word in line.split():
                yield word
    tokens = iterate_tokens()
    N = int(next(tokens))  # type: int
    a = [int(next(tokens)) for _ in range(N)]  # type: "List[int]"
    b = [int(next(tokens)) for _ in range(N)]  # type: "List[int]"
    Q = int(next(tokens))  # type: int
    x = [int()] * (Q)  # type: "List[int]"
    y = [int()] * (Q)  # type: "List[int]"
    for i in range(Q):
        x[i] = int(next(tokens))
        y[i] = int(next(tokens))
    solve(N, a, b, Q, x, y)

if __name__ == '__main__':
    main()
