#!/usr/bin/env python3
import sys

"""
def solve(N: int, M: int, A: "List[int]"):

    A_cumsum_0 = [0] * (N + 1)
    for i in range(N):
        A_cumsum_0[i + 1] = A_cumsum_0[i] + A[i]

    A_cumsum = [0] * (N + 1)
    for i in range(N):
        A_cumsum[i + 1] = A_cumsum[i] + A[i] * (i + 1)


    ans = 0
    cnt = 0
    for i in range(0, N - M + 1):
        num0 = A_cumsum_0[i + M] - A_cumsum_0[i]
        num1 = A_cumsum[i + M] - A_cumsum[i]

        num = num1 - num0 * cnt
        cnt += 1

        if ans < num:
            ans = num

    print(ans)
"""

def solve(N: int, M: int, A: "List[int]"):

    A_cumsum = [0] * (N + 1)
    for i in range(N):
        A_cumsum[i + 1] = A_cumsum[i] + A[i]

    ans_list = [0] * (N - M + 1)
    for i in range(M):
        ans_list[0] += A[i] * (i + 1)

    for i in range(0, N - M):
        ans_list[i + 1] = ans_list[i] + M * A[i + M] - (A_cumsum[i + M] - A_cumsum[i])

    #print(A_cumsum)
    #print(ans_list)
    print(max(ans_list))



# Generated by 2.12.0 https://github.com/kyuridenamida/atcoder-tools  (tips: You use the default template now. You can remove this line by using your custom template)
def main():
    def iterate_tokens():
        for line in sys.stdin:
            for word in line.split():
                yield word
    tokens = iterate_tokens()
    N = int(next(tokens))  # type: int
    M = int(next(tokens))  # type: int
    A = [int(next(tokens)) for _ in range(N)]  # type: "List[int]"
    solve(N, M, A)

if __name__ == '__main__':
    main()
