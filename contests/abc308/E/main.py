#!/usr/bin/env python3
import sys
from collections import defaultdict
INF = float('inf')  # type: float


def solve(N: int, A: "List[int]", S: str):

    N_A = 3
    cumsum_m = [[0] * N_A for _ in range(N + 1)]
    cumsum_x = [[0] * N_A for _ in range(N + 1)]

    for i, s in enumerate(S):
        for j in range(N_A):
            cumsum_m[i + 1][j] = cumsum_m[i][j]
            cumsum_x[i + 1][j] = cumsum_x[i][j]
        if s == "M":
            cumsum_m[i + 1][A[i]] += 1
        elif s == "X":
            cumsum_x[i + 1][A[i]] += 1

    def mex(mv, ev, xv):
        s = set((mv, ev, xv))
        for i in range(4):
            if i not in s:
                return i

    ans = 0
    for i, s in enumerate(S):
        if s != "E":
            continue

        for j in range(N_A):
            for k in range(N_A):
                n_m = cumsum_m[i][j] - cumsum_m[0][j]
                n_x = cumsum_x[-1][k] - cumsum_x[i + 1][k]
                ans += n_m * n_x * mex(j, A[i], k)

    print(ans)







# Generated by 2.12.0 https://github.com/kyuridenamida/atcoder-tools  (tips: You use the default template now. You can remove this line by using your custom template)
def main():
    def iterate_tokens():
        for line in sys.stdin:
            for word in line.split():
                yield word
    tokens = iterate_tokens()
    N = int(next(tokens))  # type: int
    A = [int(next(tokens)) for _ in range(N)]  # type: "List[int]"
    S = next(tokens)  # type: str
    solve(N, A, S)

if __name__ == '__main__':
    main()
