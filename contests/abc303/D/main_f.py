#!/usr/bin/env python3
import sys


def calc(X, Y, Z, cs, cap, cnt):
    ans = 0
    if not cap:
        if cs == "a":
            if X * cnt <= Y * cnt + Z:
                ans += X * cnt
            else:
                ans += Y * cnt + Z
                cap = True
        else:
            if Y * cnt <= X * cnt + Z:
                ans += Y * cnt
            else:
                ans += X * cnt + Z
                cap = True
    else:
        if cs == "a":
            if Y * cnt <= X * cnt + Z:
                ans += Y * cnt
            else:
                ans += X * cnt + Z
                cap = False
        else:
            if X * cnt <= Y * cnt + Z:
                ans += X * cnt
            else:
                ans += Y * cnt + Z
                cap = True
    return ans, cap

def solve(X: int, Y: int, Z: int, S: str):

    cap = False

    cs = S[0]
    cnt = 0

    ans = 0

    for s in S:
        if cs == s:
            cnt += 1
        else:
            ret, cap = calc(X, Y, Z, cs, cap, cnt)
            ans += ret
            cs = s
            cnt = 1

    ret, cap = calc(X, Y, Z, cs, cap, cnt)
    ans += ret
    print(ans)


# Generated by 2.12.0 https://github.com/kyuridenamida/atcoder-tools  (tips: You use the default template now. You can remove this line by using your custom template)
def main():
    def iterate_tokens():
        for line in sys.stdin:
            for word in line.split():
                yield word
    tokens = iterate_tokens()
    X = int(next(tokens))  # type: int
    Y = int(next(tokens))  # type: int
    Z = int(next(tokens))  # type: int
    S = next(tokens)  # type: str
    solve(X, Y, Z, S)

if __name__ == '__main__':
    main()
