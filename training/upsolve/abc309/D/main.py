#!/usr/bin/env python3
import sys
from collections import defaultdict
INF = float('inf')  # type: float


from heapq import heappop, heappush

def dijkstra(edges, n_node, start):
    #dist = [float('Inf')] * n_node
    #used = [False] * n_node
    dist = defaultdict(lambda: float('Inf'))
    used = defaultdict(bool)

    dist[start] = 0
    used[start] = True
    queue = [(dist[start], start)]

    while queue:
        _, now = heappop(queue)
        used[now] = True
        for next, weight in edges[now]:
            if (not used[next]) and (dist[now] + weight < dist[next]):
                dist[next] = dist[now] + weight
                heappush(queue, (dist[next], next))
    return dist


def solve(N: "List[int]", M: int, A: "List[int]", B: "List[int]"):

    edges1 = {i + 1:[] for i in range(N[0])}
    edges2 = {N[0] + j + 1: [] for j in range(N[1])}

    for a, b in zip(A, B):
        if a in edges1:
            edges1[a].append((b, 1))
            edges1[b].append((a, 1))
        else:
            edges2[a].append((b, 1))
            edges2[b].append((a, 1))

    route1 = dijkstra(edges1, N[0], 1)
    route2 = dijkstra(edges2, N[1], N[0] + N[1])

    max1 = max(route1.values())
    max2 = max(route2.values())
    print(max1 + max2 + 1)


# Generated by 2.12.0 https://github.com/kyuridenamida/atcoder-tools  (tips: You use the default template now. You can remove this line by using your custom template)
def main():
    def iterate_tokens():
        for line in sys.stdin:
            for word in line.split():
                yield word
    tokens = iterate_tokens()
    N = [int(next(tokens)) for _ in range(2)]  # type: "List[int]"
    M = int(next(tokens))  # type: int
    a = [int()] * (M)  # type: "List[int]"
    b = [int()] * (M)  # type: "List[int]"
    for i in range(M):
        a[i] = int(next(tokens))
        b[i] = int(next(tokens))
    solve(N, M, a, b)

if __name__ == '__main__':
    main()
